# Tuple
# Tuples are immutable
# when you don't want to change the info
# modification of elements are not allowed
person = ("Ganesh", "India", 20)
print(person[0])
# remove() -> pop()
# insert()-> append()

# Available  methods
# count(), index()
# index - error  if not found | find -  -1 if not found
